<%= simple_form_for([@folder, @upload_receipt]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :file_name %>
    <%= f.association :folder %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
